export async function onRequest(context) {
  // Copy the entire daily-blog.js function code here
  // This lets you test by visiting /test-blog
  
  // Same code as daily-blog.js but accessible via URL
}